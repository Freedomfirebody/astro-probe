[package]
name = "astro-probe-core"
version = "0.1.0"
edition = "2021"

[dependencies]
astro-probe-db = { path = "../astro-probe-db" }
rusqlite = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
serde = { workspace = true, features = ["derive"] }
